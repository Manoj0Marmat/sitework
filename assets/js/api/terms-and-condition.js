class TermsAndCondition {
  getTermsAndCondition(url) {
    // request to get category
    // let data = api.getData(url);
    // response dummy data
    let data = [{ terms: "Detailed terms and condition" }];
  }
}

let termsAndCondition = new TermsAndCondition();
