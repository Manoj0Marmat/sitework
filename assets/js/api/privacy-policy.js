class PrivacyPolicy {
  privacyPolicy(url) {
    // request to get category
    // let data = api.getData(url);
    // response dummy data
    let data = [{ privacy: "Detailed privacy policy" }];
  }
}

let privacyPolicy = new PrivacyPolicy();
